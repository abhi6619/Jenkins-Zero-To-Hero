apiVersion: v1
kind: Service
metadata:
  name: todo-service
  namespace: vintage-car
spec:
  ports:
    - nodePort: 32000
      port: 80
      protocol: TCP
      targetPort: 8000
  selector:
    app: nginx
  type: NodePort
